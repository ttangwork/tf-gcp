{{- range $name, $team := .Values.teams }}
---
apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: {{ $name }}
  labels:
    app: nginx
    service: nginx
spec:
  ports:
  - port: 8080
    targetPort: 80
    name: http
  selector:
    app: nginx
{{- end }}
