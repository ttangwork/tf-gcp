apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: beta-http-route
  namespace: beta
spec:
  parentRefs:
  - name: ext-gateway
    namespace: platform
    sectionName: http-8080
  hostnames:
  - "beta.example.com"
  rules:
  - matches:
    - path:
        type: Exact
        value: /
    backendRefs:
    - name: nginx
      port: 8080
      weight: 100
