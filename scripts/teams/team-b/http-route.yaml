apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: team-b-http-route
  namespace: team-b
spec:
  parentRefs:
  - name: ext-gateway
    namespace: platform
    sectionName: http

  rules:
  - matches:
    - path:
        type: Exact
        value: /team-b
    backendRefs:
    - name: nginx
      port: 8080
      weight: 100
