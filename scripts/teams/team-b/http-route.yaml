apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: team-b-http-route
  namespace: team-b
spec:
  parentRefs:
  - name: ext-gateway
    namespace: platform
    sectionName: http-8080
  hostnames:
  - "team-b.example.com"
  rules:
  - matches:
    - path:
        type: Exact
        value: /
    backendRefs:
    - name: nginx
      port: 8080
      weight: 100
